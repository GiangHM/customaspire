@copilot This PR has merge conflicts with the `main` branch, likely caused by PR #15 (Implement SQLite Storage — Logs) which was recently merged. Please resolve the merge conflicts by rebasing or merging `main` into this branch. The conflicts are likely in files like `SqliteTelemetryStorage.cs`, `DashboardOptions.cs`, `DashboardWebApplication.cs`, and/or `TelemetryRepository.cs`. Make sure the trace persistence implementation works alongside the log persistence that was already merged.